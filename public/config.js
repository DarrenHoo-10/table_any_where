window.SFG_CONFIG = {
  visual: {
    // Use "red_wood_tray" locally to preview the new red felt wooden table.
    // Keep "classic" as the default production table.
    tableTheme: 'classic',
  },
};
