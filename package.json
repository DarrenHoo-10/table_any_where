{
  "name": "straight-flush-game-web",
  "version": "1.0.0",
  "private": true,
  "description": "No-database online three-card poker web game.",
  "scripts": {
    "start": "node server/index.js",
    "server": "node server/index.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server/index.js && node --check server/roomManager.js && node --check server/rules.js && node --check public/app.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
